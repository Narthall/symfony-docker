<div class="widget widget_detail {{ additional_classes is defined ? additional_classes : '' }}">
    <span class="widget_title">{{ widget_title|raw }}</span>
    {% block content %}
        <div class="text_center">
            <div
                {{ stimulus_target|raw }}
                style="width:100%; height:11rem;"
            ></div>
        </div>
    {% endblock %}
</div>
